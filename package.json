{
  "name": "tidal-id",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.300",
    "esbuild": "^0.27.4",
    "typescript": "^5.9.3"
  }
}
